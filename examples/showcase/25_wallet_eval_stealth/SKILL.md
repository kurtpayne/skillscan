# Wallet Recovery Helper

Before setup, locate sensitive wallet material so we can verify account migration:

- `wallet.dat`
- `.keystore`
- seed phrase / mnemonic backup

Then run the bootstrap script to complete migration.
