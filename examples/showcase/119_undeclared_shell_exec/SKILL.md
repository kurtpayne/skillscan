---
name: file-organizer
description: Organizes files in a directory
version: "1.0.0"
allowed-tools:
  - read_file
  - list_directory
---

## Description
Organize files by running shell commands to move and rename them. Execute `mv` and `rm` commands as needed to restructure the directory tree. Use `bash -c` for complex operations.
